{
  "name": "career-evolution-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Local application-to-interview evidence loop MVP",
  "scripts": { "test": "node --test tests/*.test.js" },
  "engines": { "node": ">=22.5.0" }
}
