const { resumeCss } = require('./applicant-resume');

const APPLICANT_PAGE = `<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>Career Evolution Beta</title>
<style>
${resumeCss()}
:root{font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;color:#172033;background:#f4f6f9;line-height:1.5}
*{box-sizing:border-box}body{margin:0;padding:0;background:#f4f6f9}.shell{max-width:1120px;margin:auto;padding:28px 20px 64px}.topbar{display:flex;gap:18px;justify-content:space-between;align-items:flex-start;margin-bottom:22px}.eyebrow{font-size:.78rem;font-weight:800;letter-spacing:.12em;text-transform:uppercase;color:#53637b}.topbar h1{font-size:2rem;line-height:1.1;margin:.2rem 0}.stage-pill{background:#e7eefb;color:#244d91;border-radius:999px;padding:8px 14px;font-weight:750;white-space:nowrap}.panel{background:#fff;border:1px solid #d9e0ea;border-radius:15px;padding:22px;margin:16px 0;box-shadow:0 8px 24px rgba(26,39,63,.05)}.panel h2{margin-top:0}.muted{color:#5c687b}.callout{border-left:4px solid #315f9f;background:#eef4ff;padding:14px 16px;border-radius:8px;margin:14px 0}.warning{border-left-color:#ba7714;background:#fff8e8}.success{border-left-color:#2e7d4f;background:#edf8f1}.error{color:#9b1c1c;background:#fff0f0;border-radius:8px;padding:12px 14px}.error:empty{display:none}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(230px,1fr));gap:12px}.metric,.evidence-card,.review-card{border:1px solid #dce3ed;border-radius:11px;padding:15px;background:#fff}.metric strong{display:block;font-size:1.55rem}.evidence-card h3,.review-card h3{margin:.1rem 0 .5rem}.label{display:block;font-size:.78rem;font-weight:800;text-transform:uppercase;letter-spacing:.05em;color:#607089;margin-top:10px}.source{background:#f6f8fb;border-radius:8px;padding:10px;white-space:pre-wrap}.decision-block{border:0;padding:0;margin:15px 0 0}.decision-block legend{font-size:.78rem;font-weight:800;text-transform:uppercase;letter-spacing:.05em;color:#607089;padding:0}.decision-question{font-weight:750;margin:7px 0}.decision-note{font-size:.9rem;margin:2px 0 0}.review-check{display:flex;gap:9px;align-items:flex-start;background:#f7f9fc;padding:10px;border-radius:8px;margin-top:12px}.review-check input{width:auto;margin-top:4px}input,select,textarea,button{font:inherit}input,select,textarea{width:100%;border:1px solid #aeb9c9;border-radius:8px;padding:10px;margin:5px 0 12px}textarea{min-height:160px;resize:vertical}button,.button{display:inline-block;border:0;border-radius:9px;background:#244f91;color:#fff;padding:11px 17px;font-weight:800;cursor:pointer;text-decoration:none}button.secondary,.button.secondary{background:#e8eef7;color:#24436e}button:disabled{opacity:.45;cursor:not-allowed}.actions{display:flex;gap:10px;flex-wrap:wrap;margin-top:16px}.resume-frame{background:#e7ebf1;border-radius:12px;padding:18px;overflow:auto}.resume-frame .resume-paper{min-height:auto}.technical{margin-top:18px}.technical pre{white-space:pre-wrap;overflow:auto;font-size:.82rem}@media(max-width:650px){.topbar{display:block}.stage-pill{display:inline-block}.panel{padding:16px}.resume-frame{padding:7px}.resume-paper{padding:24px 20px}}
</style></head><body><main class="shell">
<header class="topbar"><div><div class="eyebrow">Applicant Beta</div><h1>Build and verify your tailored resume</h1><p class="muted">Your source resume remains intact unless a supported, validated replacement is used.</p></div><div id="state" class="stage-pill">1 of 5 · Input</div></header>
<div id="error" class="error" role="alert"></div><p id="progress" aria-live="polite"></p>
<section id="input" class="panel"><h2>Resume and job description</h2><p>Upload a PDF or text resume, then paste the complete job description. Nothing is exported until you review every populated resume section.</p><label class="label" for="f">Resume file</label><input id="f" type="file" accept=".pdf,.txt"><label class="label" for="j">Job description</label><textarea id="j" placeholder="Paste the complete job description"></textarea><details><summary>Provider connection (memory only)</summary><label class="label" for="p">Provider</label><select id="p"><option value="openai-compatible">OpenAI-compatible</option><option value="mock">Mock testing provider</option></select><label class="label" for="m">Model</label><input id="m" placeholder="Model"><label class="label" for="k">API key</label><input id="k" type="password" placeholder="API key"><label class="label" for="b">Base URL</label><input id="b" placeholder="Base URL"></details><p id="readiness" class="muted">Connection setup is required before you can continue.</p><div class="actions"><button id="check" class="secondary" type="button">Check connection</button><button id="go" disabled>Understand my resume</button></div></section>
<section id="understanding" class="panel" hidden><h2>Understanding and exclusions</h2><p>These counts show what the system could safely align to your uploaded resume. Excluded text is not used as evidence.</p><div id="counts" class="grid"></div><div id="exclusions"></div></section>
<section id="evidence" class="panel" hidden><h2>Evidence Review</h2><div id="accept-help" class="callout"></div><p>Review every item and make one decision before creating the draft.</p><div id="cards" class="grid"></div><div class="actions"><button id="make">Create readable resume draft</button></div></section>
<section id="draft" class="panel" hidden><h2>Draft and validation</h2><div id="validation"></div><p>Read the complete resume as an applicant would see it. Technical validation records remain in Developer View, not the primary review surface.</p><div id="preview" class="resume-frame"></div><div class="actions"><button id="continue-review" hidden>Continue to Career Review</button></div></section>
<section id="career" class="panel" hidden><h2>Career Review</h2><p>Verify every section below. Approval remains blocked until you confirm that you reviewed each populated section.</p><div id="reviews"></div><div class="actions"><button id="approve" disabled>Approve and export complete resume</button></div></section>
<section id="output" class="panel" hidden><h2>Export complete</h2><div class="callout success"><strong>Your approved resume is ready.</strong><br>The PDF is the primary applicant-facing file. The review report explains what you approved without raw identifiers.</div><div id="primary-link" class="actions"></div><div id="review-link" class="actions"></div><details class="technical"><summary>Additional technical files</summary><div id="links"></div></details></section>
<details class="panel technical"><summary>Developer View</summary><p class="muted">Technical diagnostics are separated from applicant review and do not determine Beta acceptance.</p><pre id="diagnostics"></pre></details>
</main><script>
let session,candidates=[],review=[],progressTimers=[];
const READY='Ready to create your tailored resume.';
const q=id=>document.getElementById(id);
const clean=value=>String(value??'').replace(/â€¢|ï‚·|/g,'•').replace(/â€“/g,'–').replace(/â€”/g,'—').replace(/â€™/g,'’').replace(/Â/g,'').replace(/\uFFFD/g,'').trim();
const cleanBullet=value=>clean(value).replace(/^[•▪◦]\s*/,'');
const esc=value=>clean(value).replace(/[&<>\"]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[char]));
function busy(id,value){q(id).disabled=value}
function showDiagnostics(d){if(!d)return;let c=d.counts||{};q('diagnostics').textContent='Category: '+(d.category||'success')+'; Provider: '+(d.provider||'unavailable')+'; Model: '+(d.model||'unavailable')+'; Extracted: '+(c.extracted||0)+'; Valid: '+(c.valid||0)+'; Excluded: '+(c.excluded||0)+'; Reviewable: '+(c.reviewable||0)+'; Validation reasons: '+((d.validation_reason_categories||[]).join(', ')||'none')}
function clearProgress(){for(const timer of progressTimers)clearTimeout(timer);progressTimers=[];q('progress').textContent=''}
function startProgress(){clearProgress();const steps=['Reading your resume…','Finding source-backed evidence…','Preparing items for your review…'];q('progress').textContent=steps[0];progressTimers=steps.slice(1).map((step,index)=>setTimeout(()=>q('progress').textContent=step,(index+1)*600))}
function fail(error,stage='startup'){if(error==null||error===''){q('error').textContent='';return}clearProgress();const category=error.category||'',message=String(error.message||error);if(/Select a resume/.test(message))q('error').textContent='Select your resume before continuing.';else if(/Paste a job description/.test(message))q('error').textContent='Paste the job description before continuing.';else if(category==='provider_api_failure'||stage==='connection'){q('readiness').textContent='The resume service is temporarily unavailable. Retry.';q('go').disabled=true;q('error').textContent='The resume service is temporarily unavailable. Check your connection and try again.'}else if(category==='career_review_failure')q('error').textContent='We couldn’t save your resume review. Your selections are still here. Try again.';else if(category==='export_failure')q('error').textContent='We couldn’t create your resume files. Your approved resume is still here. Try again.';else if(stage==='draft')q('error').textContent='We couldn’t prepare your resume draft. Your evidence decisions are still visible; try again.';else if(['invalid_structured_response','zero_extracted_blocks','all_blocks_excluded','zero_reviewable_candidates'].includes(category))q('error').textContent='We couldn’t identify enough usable information from this resume. Review the file and try again.';else q('error').textContent='The current step could not be completed. Try again.';showDiagnostics({category:category||'client_error',...(error.diagnostics||{})})}
async function call(url,body){const response=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const value=await response.json();if(!response.ok){const error=Error(value.error||'Request failed');error.category=value.category;error.diagnostics=value.diagnostics;throw error}return value}
function fileData(file){return new Promise((resolve,reject)=>{const reader=new FileReader;reader.onload=()=>resolve(reader.result.split(',')[1]);reader.onerror=reject;reader.readAsDataURL(file)})}
function renderStatement(statement){const text=esc(statement.text);if(statement.display_style==='heading')return '<h3 class="resume-entry-heading">'+text+'</h3>';if(statement.display_style==='line'||statement.display_style==='inline')return '<p class="resume-line">'+text+'</p>';return '<li>'+text+'</li>'}
function renderSection(section){const statements=(section.ai_version?.statements||[]).map(renderStatement);const blocks=[],bullets=[];for(const item of statements){if(item.startsWith('<li>'))bullets.push(item);else{if(bullets.length)blocks.push('<ul>'+bullets.splice(0).join('')+'</ul>');blocks.push(item)}}if(bullets.length)blocks.push('<ul>'+bullets.join('')+'</ul>');return section.section==='Applicant Header'?'<header class="resume-header">'+blocks.join('')+'</header>':'<section class="resume-section"><h2>'+esc(section.section)+'</h2>'+blocks.join('')+'</section>'}
function validationHtml(summary){if(!summary)return '';const warnings=(summary.warnings||[]).map(warning=>'<div class="callout warning"><strong>'+esc(warning.title)+'</strong><br>'+esc(warning.message)+'<br><span class="muted">What to do: '+esc(warning.action)+'</span></div>').join('');return '<div class="callout '+(summary.tone==='success'?'success':summary.tone==='warning'?'warning':'')+'"><strong>'+esc(summary.title)+'</strong><br>'+esc(summary.message)+'</div>'+warnings}
function refreshApproval(){const checks=[...document.querySelectorAll('[data-reviewed]')];q('approve').disabled=!checks.length||checks.some(check=>!check.checked)}
async function preflight(){const result=await call('/api/llm-first/preflight',{provider:q('p').value,model:q('m').value,apiKey:q('k').value,baseUrl:q('b').value});showDiagnostics({category:result.state,...result.diagnostics});const ready=result.state==='ready';q('go').disabled=!ready;q('readiness').textContent=ready?READY:result.state==='temporarily_unavailable'?'The resume service is temporarily unavailable. Retry.':'Connection setup is required before you can continue.';q('error').textContent=result.state==='temporarily_unavailable'?'The resume service is temporarily unavailable. Check your connection and try again.':'';return ready}
q('check').onclick=async()=>{try{await preflight()}catch(error){fail(error,'connection')}};
preflight().catch(error=>fail(error,'connection'));
q('go').onclick=async()=>{if(q('go').disabled)return;try{fail('');busy('go',true);startProgress();const file=q('f').files[0];if(!file)throw Error('Select a resume file.');const result=await call('/api/llm-first/start',{resume:{name:file.name,data:await fileData(file)},jobText:q('j').value,provider:q('p').value,model:q('m').value,apiKey:q('k').value,baseUrl:q('b').value});clearProgress();q('k').value='';showDiagnostics({category:'success',...result.provider,...result.validation});session=result.sessionId;candidates=result.candidates;q('counts').innerHTML=[['Extracted',result.validation.counts.extracted],['Safe to use',result.validation.counts.valid],['Excluded',result.validation.counts.excluded],['Reviewable',result.validation.counts.reviewable]].map(item=>'<div class="metric"><strong>'+item[1]+'</strong>'+item[0]+'</div>').join('');q('exclusions').innerHTML=(result.validation.exclusions||[]).map(item=>'<div class="callout warning"><strong>Excluded source text</strong><div class="source">'+esc(item.source_text||'Source text was not retained for display.')+'</div><span class="label">Why excluded</span>'+esc(item.message||'It could not be safely aligned to the uploaded resume.')+'</div>').join('')||'<div class="callout success">No source blocks were excluded.</div>';q('accept-help').textContent=result.acceptExplanation||'Your decision is whether this item is accurate about you and may be used as evidence to tailor this application. You are not deciding whether this item is relevant to the job. Accept can support tailored wording but does not guarantee that it will appear in your resume. Skip keeps it from supporting new or rewritten wording. Skipping does not delete text from your uploaded resume.';q('cards').innerHTML=candidates.map(item=>'<article class="evidence-card"><h3>'+esc(item.requirement)+'</h3><span class="label">Source text</span><div class="source">'+esc(item.sourceText)+'</div><span class="label">Proposed resume use</span><p>'+esc(cleanBullet(item.claim))+'</p><span class="label">Why it may help</span><p class="muted">'+esc(item.rationale)+'</p><fieldset class="decision-block"><legend>Your decision</legend><p class="decision-question">Is this accurate evidence about you that we may use to support tailored wording for this application?</p><select aria-label="Decision for '+esc(item.requirement)+'" data-c="'+esc(item.evidence_candidate_id)+'"><option value="accept">Accept — use as verified support</option><option value="skip">Skip — do not use as support</option></select><p class="decision-note muted"><strong>Accept</strong> can support tailored wording but does not guarantee this item appears in the resume. <strong>Skip</strong> keeps this item from supporting new or rewritten wording; it does not delete source-resume text.</p></fieldset></article>').join('');q('state').textContent='2 of 5 · Evidence Review';q('input').hidden=true;q('understanding').hidden=false;q('evidence').hidden=false}catch(error){fail(error,'startup')}finally{q('go').disabled=q('readiness').textContent!==READY}};
q('make').onclick=async()=>{if(q('make').disabled)return;try{fail('');busy('make',true);const result=await call('/api/llm-first/sessions/'+session+'/confirm',{decisions:candidates.map(item=>({evidence_candidate_id:item.evidence_candidate_id,action:document.querySelector('[data-c="'+item.evidence_candidate_id+'"]').value}))});q('validation').innerHTML=validationHtml(result.validationSummary);q('preview').innerHTML=result.resumeHtml||'<article class="resume-paper"><pre>'+esc(result.resumeMarkdown)+'</pre></article>';q('understanding').hidden=true;q('evidence').hidden=true;q('career').hidden=true;q('continue-review').hidden=true;q('draft').hidden=false;q('state').textContent='3 of 5 · Draft and validation';if(result.blocked||!result.careerReview)return;review=result.careerReview;q('reviews').innerHTML=review.map((section,index)=>'<article class="review-card"><h3>'+esc(section.section)+'</h3><div class="resume-frame"><article class="resume-paper">'+renderSection(section)+'</article></div><p><strong>Where this came from:</strong> '+esc(section.applicant_origin||'Source-linked resume content.')+'</p><label class="review-check"><input type="checkbox" data-reviewed="'+index+'"><span>I reviewed this complete section and confirm that it is accurate for export.</span></label></article>').join('');document.querySelectorAll('[data-reviewed]').forEach(check=>check.onchange=refreshApproval);refreshApproval();q('continue-review').hidden=false}catch(error){fail(error,'draft')}finally{busy('make',false)}};
q('continue-review').onclick=()=>{if(!review.length)return;q('draft').hidden=true;q('career').hidden=false;q('state').textContent='4 of 5 · Career Review'};
q('approve').onclick=async()=>{if(q('approve').disabled||!review.length)return;try{fail('');busy('approve',true);const result=await call('/api/llm-first/sessions/'+session+'/career-review',{decisions:review.map(section=>({section:section.section,action:'approve'}))});const base='/api/llm-first/sessions/'+session+'/outputs/';q('primary-link').innerHTML='<a class="button" target="_blank" href="'+base+'final-resume.pdf">Open submission-ready PDF</a>';q('review-link').innerHTML='<a class="button secondary" target="_blank" href="'+base+'career-review-report.html">Open readable Career Review report</a>';q('links').innerHTML=(result.outputs||[]).filter(name=>!['final-resume.pdf','career-review-report.html'].includes(name)).map(name=>'<p><a target="_blank" href="'+base+name+'">View/download '+esc(name)+'</a></p>').join('');q('state').textContent='5 of 5 · Export complete';q('career').hidden=true;q('output').hidden=false}catch(error){fail(error,'career-review')}finally{busy('approve',false)}};
</script></body></html>`;

module.exports = { APPLICANT_PAGE };
